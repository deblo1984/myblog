<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <%= viewport_meta_tag %>
    <%= favicon_link_tag %>
    <title><%= content_for?(:title) ? yield(:title) : "Myblog" %></title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
  </head>
  <body>

    <%= render :partial => 'partials/header' %>
    <div class="container">
      <div class="row">  
        <div class="col-md-12">
          <%= yield %>
        </div>
      </div>
      <footer>
      <p>&copy; bintang.me 2014</p>
      </footer>
</div>
  </body>
</html>
